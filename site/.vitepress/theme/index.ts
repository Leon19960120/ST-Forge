import DefaultTheme from 'vitepress/theme'
import './custom.css'

// Lean theme: extend VitePress's default theme and apply layout/typography
// overrides from custom.css. No custom Vue components — the docs are plain
// markdown. (See ~/Tutorial_AwesomeModernCPP for the fuller themed variant.)
export default DefaultTheme
